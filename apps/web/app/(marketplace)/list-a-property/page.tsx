import { ComingSoonPage } from "@/components/marketplace/ComingSoonPage";

export default function ListAPropertyPage() {
  return (
    <ComingSoonPage
      eyebrow="Listing creation"
      heading="Property listing creation is being built"
      description="The guided listing flow, including document upload and verification, is on the way. In the meantime, reach out to us directly if you have a property to list."
    />
  );
}
